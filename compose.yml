services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=to-do-list'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
    